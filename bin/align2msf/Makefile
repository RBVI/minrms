SHELL = /bin/sh
INSTALL_PATH = .

install:
	@echo 'Note: align2msf requires python'
	-cp -f align2msf.py $(INSTALL_PATH)/align2msf

clean:
	@echo 'Nothing to clean'

distclean:	clean
	@echo 'Nothing to clean for distribution'

depend:
	@echo 'There is no make depend rule for align2msf.'
